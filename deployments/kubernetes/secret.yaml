apiVersion: v1
kind: Secret
metadata:
  name: scout-secret
  labels:
    app: scout
type: Opaque
stringData:
  # Replace with your actual database URL
  # Format: postgres://username:password@host:port/database?sslmode=disable
  DATABASE_URL: "postgres://postgres:postgres@postgres:5432/scout?sslmode=disable"
