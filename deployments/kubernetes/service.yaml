apiVersion: v1
kind: Service
metadata:
  name: scout
  labels:
    app: scout
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: scout
---
# Optional: NodePort service for external access
# apiVersion: v1
# kind: Service
# metadata:
#   name: scout-nodeport
#   labels:
#     app: scout
# spec:
#   type: NodePort
#   ports:
#   - port: 80
#     targetPort: http
#     protocol: TCP
#     name: http
#     nodePort: 30080  # Optional: specify a specific port
#   selector:
#     app: scout
---
# Optional: LoadBalancer service for external access
# apiVersion: v1
# kind: Service
# metadata:
#   name: scout-lb
#   labels:
#     app: scout
# spec:
#   type: LoadBalancer
#   ports:
#   - port: 80
#     targetPort: http
#     protocol: TCP
#     name: http
#   selector:
#     app: scout
