# Scout Environment Configuration
# Copy this file to .env and customize for your environment

# Database Configuration
DATABASE_URL=postgres://postgres:postgres@postgres:5432/scout?sslmode=disable

# Scout Configuration
COLLECTIONS_DIR=/app/collections
NEWMAN_SCRIPT_PATH=/app/newman/executor.js
INTERVAL=30s
PORT=8080

# PostgreSQL Configuration (when running standalone)
POSTGRES_DB=scout
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Optional: For production deployments
# DATABASE_URL=postgres://username:password@your-db-host:5432/scout?sslmode=require
# INTERVAL=30s
# PORT=8080
